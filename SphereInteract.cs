using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SphereInteract : MonoBehaviour {
 /*
    void Update ()
    {
        if (Input.GetTouch(0).phase == TouchPhase.Began)
        {
            Ray raycast = Camera.main.ScreenPointToRay(Input.GetTouch(0).position);
            RaycastHit raycastHit;
            if (Physics.Raycast(raycast, out raycastHit))
            {
                if (raycastHit.collider.CompareTag("Orb"))
                {
                    Debug.Log("selecting a sphere");
                    GameSphere gameSphere = raycastHit.collider.gameObject.GetComponent<GameSphere>();
                    gameSphere.SelectSphere();                    
                }
            }
        }
    }*/
}
